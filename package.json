{
  "name": "getlocation-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site GETLOCATION — réservation et paiement Stripe",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:legal": "node scripts/check-legal-placeholders.js",
    "check:vehicle-grid": "node scripts/check-vehicle-grid-sync.js"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9",
    "stripe": "^16.12.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
