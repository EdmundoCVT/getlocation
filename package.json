{
  "name": "getlocation-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site GETLOCATION — réservation et paiement Mollie",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:legal": "node scripts/check-legal-placeholders.js",
    "check:vehicle-grid": "node scripts/check-vehicle-grid-sync.js",
    "build:worker": "wrangler pages functions build --outdir=./dist/worker/"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
